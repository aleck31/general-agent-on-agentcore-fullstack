# CDK
cdk.out/
*.d.ts
.cdk.staging/
# per-deployment state written by deploy.sh (runtime/gateway/memory ids)
.cdk-state.json
# environment lookups CDK caches (AZ list per account) — same reason: it carries the
# account id and one account's topology, and a deploy can always re-resolve it
cdk.context.json

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node
node_modules/
.dev

# AgentCore Starter Toolkit
.bedrock_agentcore.yaml
.bedrock_agentcore/
.agentcore.json

# Local
*.log
.DS_Store
.env